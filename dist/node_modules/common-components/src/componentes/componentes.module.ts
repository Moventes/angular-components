import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

import { IteratorPipe } from './pipes/iterator/iterator.pipe';
import { ValueFromEnumPipe } from './pipes/value-from-enum/value-from-enum.pipe';

export * from './types/address/address-details.type';
export * from './types/address/address.type';

@NgModule({
  imports: [
    CommonModule
  ],
  declarations: [
    IteratorPipe,
    ValueFromEnumPipe
  ],
  exports: [
    IteratorPipe,
    ValueFromEnumPipe
  ]
})
export class ComponentesCommonModule { }
